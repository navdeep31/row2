	var productStatus = document.getElementById("status").innerText;
	if (productStatus === "DISCONTINUED") {
        alert('You cannot add a discontinued item.\nTo add this ensure the item is avaliable from the supplier and ammend the status from the product page');

    } 
	